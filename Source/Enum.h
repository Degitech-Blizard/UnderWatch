#pragma once
#include "DXUT.h"


enum class WINMODE
{
	FULLSCREEN = 0,
	WINDOWED = 1
};

enum SoundType
{
	ST_BACKGROUND,
	ST_EFFECT
};

enum SortingLayer
{
	Default = 0,

}; 
enum RenderMode { RM_Normal, RM_UI, RM_Billboard };
